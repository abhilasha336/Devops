install terraform,aws client-secret(configure aws resource secret key)
first init terraform by runninng -> terraform init
then hit terraform- @ that time we get set of commands , perform those commands
